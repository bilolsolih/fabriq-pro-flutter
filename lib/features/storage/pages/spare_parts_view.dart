import 'package:flutter/material.dart';

class SparePartsView extends StatelessWidget {
  const SparePartsView({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(children: [Text("SpareParts")],);
  }
}
