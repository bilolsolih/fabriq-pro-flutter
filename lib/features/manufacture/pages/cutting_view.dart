import 'package:flutter/material.dart';

class CuttingView extends StatelessWidget {
  const CuttingView({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(children: [Text("Cutting")],);
  }
}
