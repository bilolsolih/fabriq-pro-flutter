import 'package:flutter/material.dart';

class SewingView extends StatelessWidget {
  const SewingView({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(children: [Text("Sewing")],);
  }
}
