import 'package:flutter/material.dart';

class PackagingView extends StatelessWidget {
  const PackagingView({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(children: [Text("Packaging")],);
  }
}
